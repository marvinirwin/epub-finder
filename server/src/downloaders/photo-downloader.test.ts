
describe("Downloading an image file server side", () => {
  it("Downloads an image from a url and places it in the right directory", () => {
    // expect(fileExistsSync("The filename I want"))
  });
  it("Doesn't download a new file if one is already cached", () => {
    // expect(fileExistsSync("The filename I want))
  })
})
