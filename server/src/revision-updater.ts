export class RevisionUpdater<T, RevisionT extends Partial<T>> {
    constructor(
        isAllowedFn: (currentVersion: T) => Promise<boolean>
    ) {
    }
    public static SubmitRevision(

    ) {

    }
}