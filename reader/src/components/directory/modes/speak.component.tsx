import React, {useContext} from "react";
import {ManagerContext} from "../../../App";
import {ListItem} from "@material-ui/core";

export const Speak: React.FunctionComponent = ({}) => {
    const m = useContext(ManagerContext);
    return <ListItem>

    </ListItem>
}